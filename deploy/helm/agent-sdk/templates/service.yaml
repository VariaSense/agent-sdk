apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-sdk.fullname" . }}
  labels:
    app: {{ include "agent-sdk.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "agent-sdk.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
